# Preserve Vite SPA and Python backend boundary

We will keep the current Vite ESM therapist application and connect it to a FastAPI/Python backend instead of rewriting the frontend or moving the clinical pipeline into browser code. The existing repo already has useful frontend adapters and Python speech/report modules, so the product path is to harden those boundaries rather than restart the application stack.
